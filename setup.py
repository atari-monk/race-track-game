from setuptools import setup, find_packages

setup(
    name="race-track-game",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    entry_points={
        "console_scripts": [
        ],
    },
    python_requires=">=3.7",
)
